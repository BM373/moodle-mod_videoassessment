/**
 * Guide the user to the right external-video link by showing, per the
 * chosen service, an example of the video URL to paste.
 *
 * The backend resolver auto-detects the service from the pasted URL, so
 * this selector is purely a hint: it updates the URL field's
 * placeholder and a small example line whenever the service changes.
 * It exists because users pasted platform home pages
 * (e.g. https://opencast.org/) instead of an actual video link.
 *
 * @module     mod_videoassessment/external_link_service
 * @copyright  2024 Don Hinkleman (hinkelman@mac.com)
 * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
define("mod_videoassessment/external_link_service",["core/str"],(function(Str){function apply(select,hint,hintPrefix){var option=select.options[select.selectedIndex],example=option&&option.getAttribute("data-example")||"",input=document.getElementById("id_url")||document.getElementById("id_mobileurl");input&&input.setAttribute("placeholder",example),example?(hint.textContent=hintPrefix+" "+example,hint.style.display=""):(hint.textContent="",hint.style.display="none")}return{init:function(){var select=document.getElementById("id_videoservice"),hint=document.getElementById("videoservice-hint");select&&hint&&Str.get_string("videoservicehint","mod_videoassessment").then((function(hintPrefix){return apply(select,hint,hintPrefix),select.addEventListener("change",(function(){apply(select,hint,hintPrefix)})),null})).catch((function(){apply(select,hint,"e.g."),select.addEventListener("change",(function(){apply(select,hint,"e.g.")}))}))}}}));

//# sourceMappingURL=external_link_service.min.js.map